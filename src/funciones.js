function maximo(vector) 
{

    for (let i = 0; i < vector.length; i++)
    
   {
    if (vector[i] > max) 
        {
        max = vector[i];
        }
    
   }
   console.log("el maximo es :", max);
}